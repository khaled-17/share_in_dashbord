RewriteEngine On
# إذا كان الملف أو المجلد موجودًا لا تعيد توجيه الطلب
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]
# جميع الطلبات غير الموجودة → index.html (SPA fallback)
RewriteRule ^ index.html [L]
